{
  "name": "zk0-validator-set",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "deploy": "run scripts/deploy.ts",
    "hardhat:dev": "yarn hardhat --network localhost",
    "hardhat:test": "yarn hardhat --network testnet1",
    "compile": "yarn hardhat compile",
    "start": "yarn hardhat node",
    "test": "yarn hardhat test",
    "deploy:dev": "yarn hardhat:dev run scripts/deploy.ts",
    "deploy:test": "yarn hardhat:test run scripts/deploy.ts",
    "evls:dev": "yarn hardhat --network localhost evls",
    "evls:test": "yarn hardhat --network testnet1 evls",
    "evtr:dev": "yarn hardhat --network localhost evtr",
    "evtr:test": "yarn hardhat --network testnet1 evtr",
    "evet:dev": "yarn hardhat --network localhost evet",
    "evet:test": "yarn hardhat --network testnet1 evet"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "@nomiclabs/hardhat-ganache": "^2.0.1",
    "@nomiclabs/hardhat-truffle5": "^2.0.7",
    "@nomiclabs/hardhat-web3": "^2.0.0",
    "hardhat": "^2.13.0",
    "hardhat-abi-exporter": "^2.10.1",
    "hardhat-contract-sizer": "^2.8.0",
    "hardhat-deploy": "^0.11.25",
    "hardhat-deploy-ethers": "^0.3.0-beta.13",
    "hardhat-diamond-abi": "^3.0.1",
    "hardhat-gas-reporter": "^1.0.9",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5",
    "web3": "^1.8.2"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^4.8.2",
    "@openzeppelin/test-environment": "^0.1.9",
    "@openzeppelin/test-helpers": "^0.5.16",
    "ganache": "^7.7.7",
    "helpers": "^0.0.6",
    "mocha-steps": "^1.3.0",
    "save": "^2.9.0",
    "sleep-promise": "^9.1.0",
    "solidity": "^0.0.1",
    "zeppelin-solidity": "^1.12.0"
  }
}
